apiVersion: v1
kind: Service
metadata:
  name: {{ include "textdb-chart.fullname" . }}
  labels:
    {{- include "textdb-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "textdb-chart.selectorLabels" . | nindent 4 }}
